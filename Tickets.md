// get correct text for sets page
// work on select (add the list of breeds gotten from dog.ceo)
// when a user select a breed 
// rework dogframes js for Breeds (use image instead of divs)

//update readme